import {
  ActionIcon,
  Badge,
  Button,
  MantineProvider,
  Menu,
  TextInput,
  createTheme,
} from "@mantine/core";

export const probe = {
  ActionIcon,
  Badge,
  Button,
  MantineProvider,
  Menu,
  TextInput,
  createTheme,
};
