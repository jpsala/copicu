{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "surface-notifications",
  "description": "Permissions for toast notification surface.",
  "windows": ["notifications"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show"
  ]
}
